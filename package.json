{
  "name": "salesms-root",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install"
  }
}